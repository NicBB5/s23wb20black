extends layout

block content
  h1= title
  - image_names = ['liverpool.jpg', 'grizzles.jpg', 'chiefs.jpg', 'celtic.jpg', 'royals.jpg']
  - for(i = 0; i < 3; i++){
    - var rand = Math.floor(Math.random()*5)
    - var img = "/images/"+image_names[rand]
    img(src=img)
  -}
